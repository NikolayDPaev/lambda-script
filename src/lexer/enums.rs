#[derive(Debug, PartialEq, Eq, Clone)]
pub enum Token {
    Name(String),
    Str(String),
    Char(String),
    Number(String),
    Operation(Op),
    True,
    False,
    If,
    Else,
    Then,
    Assignment,
    Arrow,
    LeftBracket,
    RightBracket,
    Comma,
    LeftBoxBracket,
    RightBoxBracket,
    NonPure,
    Cons,
    Left,
    Right,
    Empty,
    Nil,
    Import,
    Read,
    Print,
}

#[derive(Debug, PartialEq, Eq, Clone)]
pub enum Op {
    And,
    Or,
    Xor,
    Negation,
    Plus,
    Minus,
    Division,
    IntDivision,
    Multiplication,
    Exponentiation,
    Modulo,
    NEq,
    Eq,
    Lt,
    Gt,
    LEq,
    GEq,
}
